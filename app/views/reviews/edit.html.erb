<div class="new-review text-center">
    <% if @review.errors.any? %>
      <div id="error_explanation" class="alert alert-danger">
        <p><%= @review.errors.count %>errors prohibited this obj from being saved</p>
      <ul>
        <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
      </div>
    <% end %>
    <h2>レビュー投稿</h2>
    <%= form_for(@review, url: review_path) do |f| %>
    <div class="new-review-body">内容<br><%= f.text_area :review_body, style: "width: 300px; height: 100px;" %></div>
    <div class="new-review-create"><%= f.submit '更新', class: "btn btn-primary" %></div>
    <% end %>
  </div>